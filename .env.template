# Variables de configuración para desarrollo
# Este archivo sirve como documentación de variables necesarias
# NO incluir valores reales aquí

# === BACKEND CONFIGURATION ===
NODE_ENV=development
PORT=4000

# JWT Secret - Generar con: openssl rand -base64 64
JWT_SECRET=your_jwt_secret_here_64_plus_characters

# Database - Obtener de Render Dashboard
DATABASE_URL=postgresql://username:password@host:port/database

# CORS Origins - URLs permitidas para requests
CORS_ORIGIN=http://localhost:5173

# Prisma Configuration
PRISMA_HIDE_UPDATE_MESSAGE=true
CHECKPOINT_DISABLE=1

# === FRONTEND CONFIGURATION ===
# API URL para cliente
VITE_API_URL=http://localhost:4000/api

# App Info
VITE_APP_NAME="Sistema de Gestión Agrícola"
VITE_APP_VERSION="1.0.0"

# === DEPLOYMENT CONFIGURATION ===
# Nombres de servicios en Render
RENDER_BACKEND_SERVICE_NAME=your-backend-service-name
RENDER_FRONTEND_SERVICE_NAME=your-frontend-service-name

# URLs de producción (se generan automáticamente)
PRODUCTION_BACKEND_URL=https://your-backend-service.onrender.com
PRODUCTION_FRONTEND_URL=https://your-frontend-service.onrender.com
